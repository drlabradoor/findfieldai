# packages/shared

Reserved for shared types used by both the API and the web app.

For the MVP we keep it empty. When the Next.js app grows, generate TypeScript
types from the FastAPI OpenAPI schema here (e.g. with `openapi-typescript`)
instead of hand-writing them.
