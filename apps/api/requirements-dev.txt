# Local-dev subset. Use this on machines where supabase/psycopg don't build
# (e.g. Python 3.14, which has no psycopg-binary wheels yet).
#
# Render uses requirements.txt (with Python 3.11 pinned in render.yaml).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlmodel>=0.0.22
qdrant-client>=1.12
httpx>=0.27
python-multipart>=0.0.12
python-dotenv>=1.0
pillow>=11.0
pytest>=8.3
pytest-asyncio>=0.24
